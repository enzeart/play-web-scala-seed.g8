{
  "$schema": "http://json-schema.org/schema",
  "id": "AppInterceptorSchema",
  "title": "Application HTTP Interceptor Schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project to configure in the workspace configuration"
    }
  }
}
